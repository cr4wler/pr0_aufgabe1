public class Aufgabe {
	public static void main(String[] args) {
		Field field;
		Mandelbrot mandelbrot;
		VTK vtk;
		
		// TODO
		// Aufruf mit den Parametern <X/Y Size> <Frames> <Output-Prefix>
		// X/Y Size = Feldgröße in Pixel; ein Wert für X als auch Y vereinfacht das Skalieren (bleibt euch überlassen ob ihr es einzeln oder als eine Variable einlest)
		// Frames = Anzahl an zu berechnenden Frames (Anzahl Outputdateien)
		// Output-Prefix = Name mit dem die Outputdateien Anfangen
		
		// Beispielaufruf <programm> 1024 1 test
		// 1024*1024 Feld, 1 Frame, test-000000.vtk output
		
		// 1. Feld initialisieren
		
		// 2. Mandelbrot initialisieren
		
		// 3. VTK initialisieren
		
		// 4. Schleife für Frames (hier am besten VTK.getFrame() nutzen, Achtung mit Inkrement!)
		{
			// 5. Schleife für Felder (kann auch z.B. in Funktion ausgelagert werden)
			{

			}
			
			// 6. Framedaten rausschreiben (VTK)
		}
		
		// (optional) 7. evtl. Destruktoren aufrufen, Konsolenoutput o.ä.
	}
}